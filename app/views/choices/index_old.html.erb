<div class="page-header">
  <h1>My choices</h1>
</div>

<!-- Responsive, Isotope-based layout example -->

<!-- <div class="row">
  <div class="col-xs-12">
    <input type="text" id="quicksearch" placeholder="Search Choices (start typing)" class="form-control">
  </div>
</div> -->

<hr>

<div class="row isotope-container">
  <% @choices.each do |choice| %>
  <% if choice.user_id == current_user.id %>
  <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 isotope-item">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">#<%= choice.rank %>: <%= User.find(choice.target_id).name %></h3>
      </div>
      <div class="panel-body">
        <dl>
          <!-- COMMENT OUT UNTIL DISCLOSURE TIME -->
          <!-- <dt>Matched?</dt>
          <% if choice.matched == false %>
          <dd>Not yet</dd>
          <% else %>
          <dd>Yes</dd>
          <% end %> -->

          <% if choice.disclose_if_no_match == false %>
          <dd>Will NOT be notified if there's no match</dd>
          <% else %>
          <dd>Will be notified even if no match</dd>
          <% end %>
        </dl>

        <div class="btn-group btn-group-justified">

          <a href="/choices/<%= choice.id %>/edit" class="btn btn-default">Edit</a>

          <a href="/delete_choice/<%= choice.id %>" class="btn btn-danger" rel="nofollow">Delete</a>

        </div>
      </div>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
<a href="/choices/new" class="btn btn-success btn-block">Add new choice</a>

<!-- You can ignore the following. It's a quick and dirty example of how to get a Pinterest-style layout with quick search. -->
<!-- JavaScript for Isotope layout and quicksearch -->
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery.imagesloaded/3.1.8/imagesloaded.pkgd.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery.isotope/2.0.0/isotope.pkgd.min.js"></script>
<script>
  $(function() {
    // quick search regex
    var qsRegex;

    // init Isotope
    var $container = $('.isotope-container');

    $container.imagesLoaded(function() {
      $container.isotope({
        itemSelector: '.isotope-item',
        layoutMode: 'masonry',
        filter: function() {
          return qsRegex ? $(this).text().match( qsRegex ) : true;
        }
      })
    });

    // use value of search field to filter
    var $quicksearch = $('#quicksearch').keyup( debounce( function() {
      qsRegex = new RegExp( $quicksearch.val(), 'gi' );
      $container.isotope();
    }, 200 ) );

  });

  // debounce so filtering doesn't happen every millisecond
  function debounce( fn, threshold ) {
    var timeout;
    return function debounced() {
      if ( timeout ) {
        clearTimeout( timeout );
      }
      function delayed() {
        fn();
        timeout = null;
      }
      timeout = setTimeout( delayed, threshold || 100 );
    }
  }
</script>

<!-- End responsive, Isotope-based layout example -->
